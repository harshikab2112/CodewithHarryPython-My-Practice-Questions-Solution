# Can you change the values inside a list which is contained in set S?
# S={8, 7, 12, "Harry", [1,2]}

S = {8, 7, 12, "Harry", [1, 2]}
print(S)

# immutable sets and Unhashable list
# sets in Python cannot contain mutable elements like lists.
